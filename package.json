{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "LibFlow - Modern Library Management System",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd backend && npx prisma db push --skip-generate && cd .. && node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install && npx prisma generate",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "db:seed": "cd backend && node seed.js",
    "db:push": "cd backend && npx prisma db push"
  },
  "keywords": ["library", "management", "prisma", "react"],
  "author": "Randheer-07",
  "license": "ISC"
}
